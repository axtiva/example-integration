{
  "name": "apollo-federation",
  "description": "Apollo federation server",
  "private": true,
  "dependencies": {
    "@apollo/gateway": "^2.0.5",
    "apollo-server": "^3.9.0",
    "graphql": "^16.5.0"
  },
  "scripts": {
    "start": "node index",
    "start-local": "PORT=8080 node index"
  }
}
